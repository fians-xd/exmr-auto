


sudo apt update && sudo apt upgrade -y && sudo fallocate -l 1G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile && echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab && free -h && reboot

sudo apt install git build-essential cmake libuv1-dev libssl-dev libhwloc-dev screen -y

____
git clone https://github.com/xmrig/xmrig.git
cd xmrig
mkdir build && cd build
cmake ..
make -j$(nproc)

____
nano config.json

____
{
    "autosave": true,
    "cpu": true,
    "pools": [
        {
            "url": "pool.supportxmr.com:3333",
            "user": "48w7GTW8KdySVcHWaw3zMWbs8kFjrhX5bezXZuDwE61zRCG2AckcDarNRbDsturqqVWcx3faiCno7DyqVnm5o8Q556RvQ8D",
            "pass": "x",
            "keepalive": true,
            "tls": false
        }
    ]
}

____
screen -S xmrig

____
./xmrig -c config.json

____
screen -r xmrig
____




.
